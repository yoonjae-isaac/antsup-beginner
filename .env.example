# 배포 도메인. metadataBase · canonical · sitemap · OG url 이 전부 이 값을 참조한다.
# 미설정 시 src/config/site.ts 의 기본값(https://step.ants-up.com)을 쓴다.
# Vercel 프리뷰 배포에서는 프리뷰 URL 로 덮어쓰면 된다.
NEXT_PUBLIC_SITE_URL=https://step.ants-up.com

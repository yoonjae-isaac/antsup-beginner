import type { NextConfig } from 'next';

// 한국어 단일 로케일 · 라우트 1개 · 외부 연동 없음 — 추가 설정이 필요 없는 상태다.
// 플러그인(next-intl 등)을 넣지 않는 것이 의도이며, 빈 설정을 유지한다.
const nextConfig: NextConfig = {};

export default nextConfig;

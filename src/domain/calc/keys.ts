/**
 * 계산기 식별자.
 *
 * 계산기는 독립된 허브를 갖지 않고 레슨 끝에 하나씩 붙는다 — 방금 읽은 내용을 그
 * 자리에서 숫자로 확인하는 게 목적이라, 따로 떼어 두면 찾아가지 않는다.
 * 그래서 이 키는 `lessons.ts` 의 레슨 한 건이 들고 있고, 화면은 그걸로 고른다.
 *
 * 타입만 여기 두는 이유는 순환 참조 때문이다 — 레슨은 이 키를 알아야 하고,
 * 계산기 컴포넌트 레지스트리는 레슨을 알아야 한다.
 */
export type CalcKey =
  | 'averaging'
  | 'stop-target'
  | 'dividend'
  | 'decay'
  | 'compound'
  | 'savings-goal';
